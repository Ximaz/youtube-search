# Nuxt dev/build outputs
.output
.output-worker
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# Real secrets (only *.env.example templates are committed)
secrets/**/*.env
!secrets/**/*.env.example

# Local container data volumes (if bind-mounted during dev)
/.volumes

# Claude local config / project guide (kept local, not committed)
.claude/
CLAUDE.md
